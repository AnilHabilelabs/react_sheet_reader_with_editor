### installation :

run install.sh script to install backend and frontend dependencies.

```
bash install.sh
```

### put all your pdf files in frontend/pdfs directory and then upload xlsx file containing file names, for example find xlsx file in the frontend/public folder.

### start

run start.sh script to start the frontend and backend server.

```
bash start.sh
```
